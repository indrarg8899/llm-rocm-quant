# LLM-ROCm-Quant
# Quantization toolkit for AMD MI300X (ROCm 6.x)

# Core
torch>=2.4.0
transformers>=4.40.0
safetensors>=0.4.0

# Quantization & model ops
accelerate>=0.30.0
peft>=0.11.0
optimum>=1.19.0

# Datasets (calibration + eval)
datasets>=2.19.0

# Config
pyyaml>=6.0

# Benchmarks & eval
tiktoken>=0.7.0
sentencepiece>=0.2.0

# ONNX export (optional)
onnx>=1.16.0
onnxruntime-gpu>=1.18.0

# ROCm-specific
triton>=2.3.0
# ROCm HIP SDK 6.2+ required system-wide
# Install: sudo apt install rocm-hip-runtime rocm-dev

# Testing
pytest>=8.0.0
pytest-xdist>=3.5.0

# CLI
rich>=13.0.0
click>=8.1.0
